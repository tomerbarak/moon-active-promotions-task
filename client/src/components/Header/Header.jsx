import React from "react";
import classes from "./Header.scss";

const Header = () => {
  return (
    <div className={classes.container}>
      <h1 className={classes.header}>The New Promotion Screen</h1>
    </div>
  );
};

export default Header;
